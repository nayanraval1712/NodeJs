// we cre a object for export multipal function 
let hasher = {
    password: function (orignalpassword) {
        let password = "bhai revade tu ......"
        return password;
    },
    matchPassword: function (userGivenPassword, password) {
        let matchPassword = "ha bhai ha same j che ..."
        return matchPassword;
    }
}
// export a object 
module.exports = hasher;